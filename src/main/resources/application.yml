spring:
  datasource:
    url: jdbc:h2:mem:cmsdb;DB_CLOSE_DELAY=-1
    driverClassName: org.h2.Driver
    username: sa
    password: ''
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
server:
  port: 8080

openai:
  api-key: YOUR_API_KEY
  url: https://api.openai.com/v1/chat/completions
